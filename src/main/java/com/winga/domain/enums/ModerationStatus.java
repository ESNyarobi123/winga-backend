package com.winga.domain.enums;

/** Job moderation: Super Admin / Moderator approve or reject before job goes live. */
public enum ModerationStatus {
    PENDING_APPROVAL,
    APPROVED,
    REJECTED
}
